[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python init_db.py", "python create_test_users.py"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
